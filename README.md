# DataPlatforms

Repository này hiện đóng gói module nhánh `ETL_Market_Data` để dễ tích hợp vào hệ thống lớn hơn.

Toàn bộ code, tài liệu và dữ liệu dump của module nằm trong thư mục:

- [ETL_Market_Data](./ETL_Market_Data)

Khi làm việc với module này, hãy `cd ETL_Market_Data` rồi dùng:

- `README.md` để xem hướng dẫn đầy đủ
- `QUICKSTART.md` để chạy nhanh
- `docs/LLM_BRANCH_HANDOVER.md` để bàn giao cho hệ thống tổng
